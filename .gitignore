node_modules
# Keep environment variables out of version control
.env
.env.local
dist/
# Relatório de cobertura (pnpm test:coverage)
coverage/
.vscode/
/generated/prisma
.worktrees/

# Saídas dos testes de carga (k6) — geradas localmente
load-tests/results/

# Skills/agents locais — não vão pro repo do time
.agents/
.claude/
# Skills do Claude Code (geradas pelo harness na pasta do repo, não versionar)
skills-lock.json
